package net.masterquentus.hexcraftmod.datagen;

@Deprecated(since = "1.0", forRemoval = true)
public class HexcraftRegistrySets {
    /*
     * extends DatapackBuiltinEntriesProvider { } public static final
     * RegistrySetBuilder BUILDER = new RegistrySetBuilder() .add(Registries.NOISE,
     * ctx -> HexcraftNoises.bootstrap(ctx)) .add(Registries.NOISE_SETTINGS,
     * HexcraftNoiseSettings::bootstrap);
     *
     * public HexcraftRegistrySets(PackOutput output,
     * CompletableFuture<HolderLookup.Provider> registries) { super(output,
     * registries, BUILDER, Collections.singleton(HexcraftMod.MOD_ID)); }
     *
     * private static HolderLookup.Provider createLookup() { return
     * BUILDER.buildPatch(RegistryAccess.fromRegistryOfRegistries(BuiltInRegistries.
     * REGISTRY), VanillaRegistries.createLookup()); }
     */
}